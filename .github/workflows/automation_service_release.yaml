name: Release Automation Service Docker Image

on:
  push:
    tags: ['v*']

permissions:
  contents: write
  packages: write

jobs:
  release:
    uses: ./.github/workflows/service_release.yml
    with:
      service: automation-service
      context: .
      dockerfile: ./automation-service/Dockerfile
    secrets: inherit
