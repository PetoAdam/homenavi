name: Release Thread Adapter Docker Image

on:
  push:
    tags: ['v*']

permissions:
  contents: write
  packages: write

jobs:
  release:
    uses: ./.github/workflows/service_release.yml
    with:
      service: thread-adapter
      context: .
      dockerfile: ./thread-adapter/Dockerfile
    secrets: inherit
