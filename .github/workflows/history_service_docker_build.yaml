name: Build History Service Docker Image

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: ./history-service/go.mod
          cache-dependency-path: ./history-service/go.sum

      - name: Run unit tests
        working-directory: ./history-service
        run: go test ./...

      - name: Build the Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./history-service
          file: ./history-service/Dockerfile
          push: false
          tags: homenavi-history-service:latest

      - name: Save Docker image to tarball
        run: docker save homenavi-history-service:latest | gzip > homenavi-history-service-latest.tar.gz

      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: homenavi-history-service-latest
          path: homenavi-history-service-latest.tar.gz
