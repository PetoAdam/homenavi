name: Build Automation Service Docker Image

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: ./automation-service/go.mod
          cache-dependency-path: ./automation-service/go.sum

      - name: Run unit tests
        working-directory: ./automation-service
        run: go test ./...

      - name: Build the Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./automation-service
          file: ./automation-service/Dockerfile
          push: false
          tags: homenavi-automation-service:latest

      - name: Save Docker image to tarball
        run: docker save homenavi-automation-service:latest | gzip > homenavi-automation-service-latest.tar.gz

      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: homenavi-automation-service-latest
          path: homenavi-automation-service-latest.tar.gz
