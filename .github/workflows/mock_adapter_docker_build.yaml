name: Build Mock Adapter Docker Image

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: ./mock-adapter/go.mod
          cache-dependency-path: ./mock-adapter/go.sum

      - name: Run static analysis
        working-directory: ./mock-adapter
        run: go vet ./...

      - name: Run unit tests
        working-directory: ./mock-adapter
        run: go test ./...

      - name: Build the Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./mock-adapter/Dockerfile
          push: false
          tags: homenavi-mock-adapter:latest

      - name: Save Docker image to tarball
        run: docker save homenavi-mock-adapter:latest | gzip > homenavi-mock-adapter-latest.tar.gz

      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: homenavi-mock-adapter-latest
          path: homenavi-mock-adapter-latest.tar.gz
