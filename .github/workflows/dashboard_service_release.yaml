name: Release Dashboard Service Docker Image

on:
  push:
    tags: ['v*']

permissions:
  contents: write
  packages: write

jobs:
  release:
    uses: ./.github/workflows/service_release.yml
    with:
      service: dashboard-service
      context: ./dashboard-service
      dockerfile: ./dashboard-service/Dockerfile
    secrets: inherit
