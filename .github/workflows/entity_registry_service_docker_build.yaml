name: Build Entity Registry Service Docker Image

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: ./entity-registry-service/go.mod
          cache-dependency-path: ./entity-registry-service/go.sum

      - name: Run unit tests
        working-directory: ./entity-registry-service
        run: go test ./...

      - name: Build the Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./entity-registry-service
          file: ./entity-registry-service/Dockerfile
          push: false
          tags: homenavi-entity-registry-service:latest

      - name: Save Docker image to tarball
        run: docker save homenavi-entity-registry-service:latest | gzip > homenavi-entity-registry-service-latest.tar.gz

      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: homenavi-entity-registry-service-latest
          path: homenavi-entity-registry-service-latest.tar.gz
