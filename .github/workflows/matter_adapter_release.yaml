name: Release Matter Adapter Docker Image

on:
  push:
    tags: ['v*']

permissions:
  contents: write
  packages: write

jobs:
  release:
    uses: ./.github/workflows/service_release.yml
    with:
      service: matter-adapter
      context: .
      dockerfile: ./matter-adapter/Dockerfile
    secrets: inherit
