name: Build Thread Adapter Docker Image

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build the Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./thread-adapter
          file: ./thread-adapter/Dockerfile
          push: false
          tags: homenavi-thread-adapter:latest

      - name: Save Docker image to tarball
        run: docker save homenavi-thread-adapter:latest | gzip > homenavi-thread-adapter-latest.tar.gz

      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: homenavi-thread-adapter-latest
          path: homenavi-thread-adapter-latest.tar.gz
